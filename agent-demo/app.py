from asyncio import Runner
from openai.agents import Agent, message

runner = Runner(math_agent.agent)
session = runner.sessions.create()


{
  "title": "string",  
  "description": "string", 
  "tags": [string], 
  "ownerId": "string"  
}